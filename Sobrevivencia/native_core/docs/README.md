# Native Core

Native code is experimental and should stay optional.

Use this area for notes about C++ acceleration, build commands and benchmark results. Python fallbacks must remain available for every native feature.
