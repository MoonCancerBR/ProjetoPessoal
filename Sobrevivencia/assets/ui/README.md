# UI

Interface images, icons and window assets.

- `icons/`: commands, items, warnings and HUD icons.
- `windows/`: panels, borders, modal backgrounds and UI slices.

Theme colors and pygame_gui styling remain in `assets/theme.json`.
