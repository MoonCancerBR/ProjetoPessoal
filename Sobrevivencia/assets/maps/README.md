# Maps

Map assets, tilesets and optional Tiled files.

- `tilesets/`: tile images and atlas metadata.
- `ambient/`: decorative props, decals, light masks and background details.

The procedural world remains the fallback when optional map loaders are missing.
