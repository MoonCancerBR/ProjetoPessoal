# Multiplayer

Destino para regras de coop local:

- `revive/`
- `camera/`
- `tether/`
- `draft/`

Regras que hoje vivem em `GameLogic`, `InputManager` e menus devem migrar para
servicos pequenos daqui.

