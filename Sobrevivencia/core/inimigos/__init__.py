"""Enemy domain package.

This package is the migration target for enemy spawning, scaling, AI and
enemy-specific combat behavior. Existing managers can delegate here while the
large legacy modules are split incrementally.
"""

